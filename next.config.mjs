/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['images.unsplash.com', 'avatars.githubusercontent.com'], // Add 'images.unsplash.com' here
  },
};

export default nextConfig;
